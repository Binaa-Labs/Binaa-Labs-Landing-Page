// Reuse the same branded social card for the Arabic route (file-based
// opengraph-image does not cascade into this segment automatically).
export { default, alt, size, contentType } from "../opengraph-image";
